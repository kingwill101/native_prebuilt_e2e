native_prebuilt:build:ios:
  stage: build
  tags:
    - macos
  rules:
    - if: '$CI_COMMIT_TAG && $ENABLE_ALL_PLATFORMS == "true"'
  script:
    - dart pub get
    - "$BUILD_COMMAND"
  artifacts:
    when: always
    paths:
      - .dart_tool/lib/
