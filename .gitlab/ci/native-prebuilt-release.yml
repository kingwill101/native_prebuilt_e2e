native_prebuilt:release:
  stage: release
  image: alpine:3.20
  needs:
    - job: native_prebuilt:update_manifest
      artifacts: true
  rules:
    - if: '$CI_COMMIT_TAG'
  script:
    - echo "Release $TAG"
